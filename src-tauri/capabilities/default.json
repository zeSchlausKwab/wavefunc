{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for the main WaveFunc window",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "os:default",
    "shell:default"
  ]
}
